name: FreezerTracker
options:
  bundleIdPrefix: io.learnsharegrow.freezertracker
settings:
  base:
    IPHONEOS_DEPLOYMENT_TARGET: 16.0
    SWIFT_VERSION: 5.9

targets:
  FreezerTracker:
    type: application
    platform: iOS
    deploymentTarget: 16.0
    sources:
      - path: FreezerTracker
    settings:
      PRODUCT_BUNDLE_IDENTIFIER: io.learnsharegrow.freezertracker
      INFOPLIST_FILE: FreezerTracker/Resources/Info.plist
      ASSETCATALOG_COMPILER_APPICON_NAME: AppIcon
