export type Freezer = {
  id: number;
  name: string;
  shelfCount: number;
};

export type FreezerCreate = {
  name: string;
  shelfCount: number;
};

export type FreezerUpdate = {
  name: string;
  shelfCount: number;
};
